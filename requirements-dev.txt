# =============================================================================
# DL-RCIS 개발·검증 의존성
#
# 런타임에는 필요 없지만 테스트·정적검사에 필요한 패키지입니다.
# 실행 환경(서버·GitHub Actions 수집 작업)에는 requirements.txt 만 설치하고,
# 검증 작업에서만 이 파일을 설치하십시오.
#
#   pip install -r requirements-dev.txt
# =============================================================================

-r requirements.txt

pytest>=8.0                 # 인수 테스트 (tests/)
ruff>=0.5                   # 린트·포맷 검사
